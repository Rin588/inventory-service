name: Build & Deploy — Main

on:
  push:
    branches:
      - main

jobs:
  dockr-build-and-push:
    runs-on: ubuntu-latest
    steps:
      # 1. Upgraded to v4 to fix node deprecation warnings
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v3
        with:
          java-version: "25"
          distribution: "temurin"
          cache: maven

      - name: Extract Maven project version
        id: pom-version
        run: echo "version=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)" >> "$GITHUB_OUTPUT"

      - name: Bild tag version
        id: tag-version
        run: echo "version=${{ steps.pom-version.outputs.version }}-${{ github.run_number }}-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Maven build
        run: mvn clean install

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.MY_GITHUB_TOKEN }}

      # 2. Re-arranged order: Push image first before switching directories
      - name: Build and Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./deployment/Dockerfile.dev
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/rin588/inventory-service:${{ steps.tag-version.outputs.version }}

      # 3. Upgraded to v4. Crucial: Make sure secrets.MY_GITHUB_TOKEN is a PAT with 'repo' scope!
      - name: Checkout Kustomize chart Repository
        uses: actions/checkout@v4
        with:
          repository: rin588/workshop-apps
          path: deployment
          token: ${{ secrets.MY_GITHUB_TOKEN }}

      # 4. Verified and corrected the Kustomize updates section
      # - name: Update image tag in dev overlay
      #   uses: fjogeleit/yaml-update-action@main
      #   with:
      #     repository: rin588/workshop-apps  # Changed from zawthanoo to match your repo structure
      #     valueFile: inventory-service/overlays/dev/kustomization.yaml
      #     branch: main
      #     createPR: "false"
      #     message: "inventory-service dev release for workshop"
      #     token: ${{ secrets.MY_GITHUB_TOKEN }}
      #     workDir: deployment
      #     changes: |
      #       {
      #         "images[0].newTag": "${{ steps.tag-version.outputs.version }}"
      #       }
